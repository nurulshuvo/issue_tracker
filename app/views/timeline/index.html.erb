<h1>Timeline</h1>

<table>
	<% @timelines.each do |timeline| %>
		<tr>
			<td><%= timeline.content %></td>
			<td><%= timeline.timelineable_type %></td>
			<td><%= time_ago_in_words(timeline.created_at) %></td>
		</tr>
	<% end %>
</table>
